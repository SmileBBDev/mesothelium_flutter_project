# ========================================
# 🔒 환경 변수 예시 파일
# ========================================
# 이 파일을 복사하여 실제 값을 입력하세요
#
# 사용 방법:
# 1. 이 파일을 복사: cp .env.example .env.local
# 2. .env.local에 실제 API 키 입력
# 3. 아래 스크립트로 환경 변수 로드하여 빌드
#
# ========================================

# 환경 설정 (development, staging, production)
ENVIRONMENT=development

# API 서버 URL
API_BASE_URL=http://localhost:8000

# vWorld 지도 API 키 (https://www.vworld.kr/에서 발급)
# 🔴 실제 키를 입력하세요!
VWORLD_API_KEY=여기에_실제_API_키_입력

# ========================================
# 빌드 스크립트 예시
# ========================================
#
# 개발 환경 실행:
#   flutter run --dart-define=ENVIRONMENT=development \
#               --dart-define=API_BASE_URL=http://localhost:8000 \
#               --dart-define=VWORLD_API_KEY=실제키값
#
# 프로덕션 빌드:
#   flutter build apk --release \
#                     --dart-define=ENVIRONMENT=production \
#                     --dart-define=API_BASE_URL=https://api.production.com \
#                     --dart-define=VWORLD_API_KEY=실제키값
#
# ========================================
