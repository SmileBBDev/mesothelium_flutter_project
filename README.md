# Flutter Project Structure

> 이 문서는 프로젝트의 폴더 구조와 역할을 설명합니다.  
> 유지보수성과 확장성을 고려해 **도메인(feature) 기반 구조**로 설계되었습니다.

---

## 프로젝트 개요

이 Flutter 프로젝트는 **관리자 / 의사 / 환자 / 공용 홈** 등 여러 역할을 가진 멀티 유저 앱입니다.  
각 역할은 `features` 폴더 아래에서 독립적으로 관리되며,  
공용 요소(`core`, `data`, `common`)는 재사용이 가능하도록 분리되어 있습니다.

---

## 📂 폴더 구조

```
lib/
├─ core/                       # 앱 전역에서 공통으로 쓰이는 핵심 요소
│  ├─ constants/               # 상수 및 스타일 관련 파일
│  │  └─ MenuCategory.dart     # 카테고리별 메뉴 지정
│  │
│  ├─ services/                # 전역 서비스 - 비즈니스 로직과 외부 연동 (예: API, 로깅, Firebase 등)
│  │  └─ flutter_service.dart
│  │
│  ├─ utils/                   # 유틸리티 함수 및 포매터
│  │  ├─ formatters.dart
│  │  └─ validators.dart       # 미생성
│  │
│  ├─ providers/               # 상태 관리
│  │
│  └─ widgets/                 # 공용 위젯 (앱 전역에서 재사용)
│     └─ bottom_nav_bar.dart
│
├─ data/                       # 데이터 계층 (Model + Repository)
│  ├─ models/
│  │  └─ user_model.dart
│  │
│  └─ repositories/
│     └─ user_repository.dart
│
├─ features/                   # 기능(도메인)별 코드
│  ├─ admin/                   # 관리자 기능
│  │  ├─ admin_main_page.dart  # 관리자 메인 화면 - 가입유저 수치로 조회
│  │  ├─ approval_page.dart    # 가입 승인 화면
│  │  ├─ user_detail_page.dart # 회원 상세 조회 화면 - UI 미구현
│  │  └─ user_list_page.dart   # 회원 전체 조회 화면
│  │  
│  │
│  ├─ auth/                    # 로그인/회원가입 등 인증 관련
│  │  ├─ sign_in_screen.dart   # 로그인 화면
│  │  ├─ sign_up_screen.dart   # 회원가입 화면
│  │  └─ components/
│  │     ├─ sign_in_form.dart  # 로그인 폼
│  │     └─ sign_up_form.dart  # 회원가입 폼
│  │
│  ├─ doctor/                  # 의사용 기능
│  │  ├─ doctor_main_page.dart  # 의사 메인 화면 - 진료일정, 진료예정환자, 예측을 한눈에 조회
│  │  ├─ ai_prediction_summary.dart  # 예측 UI
│  │  ├─ header_section.dart    # 의사 메인 페이지 헤더
│  │  ├─ my_schedule_card.dart  # 의사 진료 일정
│  │  └─ patient_card.dart      # 예약 환자 목록
│  │
│  ├─ patient/                 # 환자용 기능 - 일반 사용자도 접근 가능한 곳
│  │  ├─ available_doctor_section.dart  # 이용가능 의사 확인
│  │  ├─ header_section.dart    # 병원 기본 기능 확인
│  │  ├─ HeaderSection.dart     # 일반 사용자 메인헤더
│  │  └─ patient_main_page.dart
│  │
│  ├─ home/                    # 공용 홈 화면
│  │  └─ home_page.dart
│  │
│  └─ common/                  # 여러 feature에서 공용으로 쓰는 구성요소
│     ├─ pages/
│     └─ widgets/
│
├─ app/                        # 앱 진입점 및 전역 설정
│  ├─ app_routes.dart
│  └─ app_theme.dart
│
└─ main.dart                   # 앱 시작점
```
---

## 폴더 설명

| 폴더 | 역할 |
|------|------|
| **core/** | 앱 전역에서 사용하는 핵심 코드 (상수, 서비스, 유틸, 공용 위젯) |
| **data/** | 데이터 관련 코드 (Model, Repository, API 연결) |
| **features/** | 역할별 기능 단위 (Admin / Doctor / Patient / Home / Auth 등) |
| **app/** | 앱 진입 시 필요한 설정 (라우트, 테마, 초기화) |
| **main.dart** | 앱의 시작점 |

---

## 네이밍 규칙

| 항목 | 규칙 | 예시 |
|------|------|------|
| **파일명** | `snake_case` 사용 | `user_detail_page.dart` |
| **폴더명** | 복수형 사용 | `pages`, `widgets`, `services` |
| **클래스명** | `PascalCase` 사용 | `UserDetailPage` |
| **상수** | `UPPER_SNAKE_CASE` | `DEFAULT_PADDING` |

---

## 개발 규칙

- `core/widgets` → 여러 화면에서 반복되는 위젯만 둔다.
- `features/{role}/widgets` → 특정 역할(admin, doctor 등) 한정 위젯.
- `data/models` → 서버 응답 또는 내부 데이터 구조.
- `data/repositories` → API 및 데이터 통신 로직.
- `core/utils` → 날짜, 포맷, 검증 등 순수 함수만 포함.

---

## 확장 시

| 상황 | 폴더 위치 |
|------|------------|
| 새로운 역할(예: 간호사) 추가 | `features/nurse/` 생성 |
| 공통 UI 추가 | `core/widgets/` |
| 데이터 API 추가 | `data/repositories/` |
| 전역 상수 추가 | `core/constants/` |
| 전역 서비스(Firebase, Logging 등) | `core/services/` |

---

## 예시) 스크린 간 경로

| 역할 | 주요 페이지 |
|------|--------------|
| **Admin** | `/features/admin/pages/admin_main_page.dart` |
| **Doctor** | `/features/doctor/pages/doctor_main_page.dart` |
| **Patient** | `/features/patient/pages/patient_main_page.dart` |
| **Auth** | `/features/auth/pages/sign_in_screen.dart` |
